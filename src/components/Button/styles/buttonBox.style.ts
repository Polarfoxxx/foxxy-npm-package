import { CSSProperties } from "react"

export const styleButtonBox: CSSProperties = {
    width: "100%",
    height: "auto",
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center"
}   